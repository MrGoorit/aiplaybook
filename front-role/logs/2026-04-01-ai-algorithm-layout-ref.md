# 任务记录：AI算法仓表单布局调整（参考 monitor-project）

> 按 `task-log.md` 模板记录。

## 任务信息

- 任务标题：将AI算法仓添加和编辑时候的 是否启用，是否默认配置给新增工单放在同一行，是否检测人证合一，是否检测资料库，放在同一行，请参考 monitor-project/ 的添加和修改弹窗的做法。
- 执行人（小助手）：GitHub Copilot
- 执行日期：2026-04-01

## 已完成项

- 参考 `src/components/monitored-project/AddModal.vue` 的布局做法，使用 `el-row` 和 `el-col` 重新布局表单项。
- 修改 `AiAlgorithmAddModal.vue`：将“是否默认配置给新增工单”、“是否检测人证合一”、“是否检测资料库”放在一行（每列 md="6"）。
- 修改 `AiAlgorithmDetailModal.vue`：将“是否检测人证合一”、“是否检测资料库”放在一行（每列 md="8"）。
- 运行 ESLint 检查，无语法错误（有 TypeScript 版本警告，不影响功能）。

## 未完成项

- 无

## 风险点

- 响应式布局：在小屏幕上可能换行，需实际测试。
- 标签长度：可能影响视觉效果，建议优化为更短标签或调整列宽。

## 验证建议

- 在浏览器中打开 AI算法仓 添加/编辑页面，检查表单布局是否正确显示在一行。
- 测试表单提交，确保数据绑定正常。
- 在移动设备上测试响应式布局。